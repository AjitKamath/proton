<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/common_spinner_ll"
    android:layout_width="match_parent"
    android:gravity="center"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <ImageView
        android:layout_width="@dimen/spinner_img_size"
        android:layout_height="@dimen/spinner_img_size"
        android:layout_marginRight="5dp"
        android:background="@drawable/ic_list_grey600_48dp" />

    <!--<ImageView
        android:id="@+id/common_spinner_iv"
        android:layout_width="@dimen/spinner_img_size"
        android:layout_height="@dimen/spinner_img_size"
        android:background="@drawable/seek_book" />-->

    <TextView
        android:id="@+id/common_spinner_tv"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="2dp"
        android:layout_marginRight="2dp"
        android:text="THRILLER"
        android:textColor="@color/textLightTertiary"
        android:textSize="@dimen/spinner_text_size" />

</LinearLayout>