<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/common_amount_ll"
    android:layout_width="@dimen/form_fields_width"
    android:gravity="center"
    android:background="@drawable/edit_text_border_dark"
    android:layout_gravity="center"
    android:layout_height="wrap_content"
    android:orientation="horizontal">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="₹"
        android:padding="5dp"
        android:gravity="center"
        android:layout_gravity="center"
        android:textColor="@color/text_currency"
        android:textSize="@dimen/spinner_text_size" />

    <EditText
        android:id="@+id/share_rent_et"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:text="0"
        android:textColor="@color/appPrimaryLight"
        android:inputType="numberDecimal"
        android:padding="5dp"
        android:textSize="15sp" />

</LinearLayout>